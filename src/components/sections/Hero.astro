---
import { links } from '@/data/projects';
---

<div class="hero-minimal relative flex flex-col items-center justify-center min-h-[calc(100vh-120px)] px-6 py-20 z-10 overflow-hidden">
  <!-- Beams Effect -->
  <div class="hero-beams absolute inset-0 z-0 pointer-events-none"></div>
  
  <div class="flex flex-col items-center text-center max-w-4xl relative z-10">
    
    <!-- Badge Superior -->
    <p class="text-[0.65rem] font-normal uppercase tracking-[0.25em] text-gray-500 mb-8">
      <span data-blur-reveal data-delay="0" data-stagger="0.05">Diseño Web · IA · Automatización</span>
    </p>
    
    <!-- Título Principal -->
    <h1 class="text-[clamp(2rem,5vw,4.5rem)] font-normal leading-[1.1] text-[#1a1a1a] mb-6 max-w-[800px]">
      <span data-blur-reveal data-delay="0.2" data-stagger="0.06">Diseño la tecnología invisible que hace visibles tus resultados.</span>
    </h1>
    
    <!-- Párrafo -->
    <p class="text-[clamp(1rem,2vw,1.25rem)] font-light leading-relaxed text-[#666] max-w-[600px] mt-6">
      <span data-blur-reveal data-delay="1.2" data-stagger="0.04">Creo sistemas completos que integran diseño estratégico, automatización de procesos y agentes de IA. Todo bajo tu marca, todo conectado, todo funcionando 24/7.</span>
    </p>
    
    <!-- CTA Button -->
    <a 
      href={links.calendar}
      target="_blank"
      rel="noopener noreferrer"
      class="blur-reveal-word inline-flex items-center gap-2 bg-[#1A1A1A] text-white text-sm font-medium uppercase tracking-widest rounded-full px-6 py-3.5 mt-8 hover:bg-indigo-600 hover:scale-105 transition-all shadow-lg"
      style="animation-delay: 2.5s; opacity: 0;"
    >
      Agendar ConsultorIA
      <i data-lucide="arrow-up-right" class="w-4 h-4"></i>
    </a>
    
  </div>
</div>

<style>
  .hero-minimal h1 {
    font-family: var(--font-display);
    font-weight: 400;
  }
  
  .hero-minimal p {
    font-family: var(--font-body);
  }
  
  .hero-minimal a {
    font-family: var(--font-body);
    font-weight: 500;
  }
  
  /* Beams Effect */
  .hero-beams {
    opacity: 0.15;
  }
  
  .hero-beams::before,
  .hero-beams::after {
    content: '';
    position: absolute;
    width: 200%;
    height: 200%;
    background: linear-gradient(
      45deg,
      transparent 0%,
      rgba(255, 107, 53, 0.1) 25%,
      rgba(247, 147, 26, 0.15) 50%,
      rgba(255, 179, 71, 0.1) 75%,
      transparent 100%
    );
    animation: beam-rotate 18s ease-in-out infinite;
  }
  
  .hero-beams::before {
    top: -50%;
    left: -50%;
    transform-origin: center center;
  }
  
  .hero-beams::after {
    bottom: -50%;
    right: -50%;
    transform-origin: center center;
    animation-delay: -9s;
    background: linear-gradient(
      -45deg,
      transparent 0%,
      rgba(255, 107, 53, 0.1) 25%,
      rgba(247, 147, 26, 0.15) 50%,
      rgba(255, 179, 71, 0.1) 75%,
      transparent 100%
    );
  }
  
  @keyframes beam-rotate {
    0%, 100% {
      transform: rotate(0deg) scale(1);
      opacity: 0.1;
    }
    25% {
      transform: rotate(5deg) scale(1.1);
      opacity: 0.2;
    }
    50% {
      transform: rotate(0deg) scale(1);
      opacity: 0.15;
    }
    75% {
      transform: rotate(-5deg) scale(1.1);
      opacity: 0.2;
    }
  }
  
  /* Blur Reveal Animation */
  @keyframes blurReveal {
    0% {
      opacity: 0;
      filter: blur(10px);
      transform: translateY(20px);
    }
    100% {
      opacity: 1;
      filter: blur(0);
      transform: translateY(0);
    }
  }
  
  .blur-reveal-word {
    display: inline-block;
    opacity: 0;
    animation: blurReveal 0.8s ease-out forwards;
  }
</style>

<script>
  function initBlurReveal() {
    const elements = document.querySelectorAll('[data-blur-reveal]');
    
    elements.forEach((element) => {
      const text = element.textContent || '';
      const words = text.split(' ');
      const baseDelay = parseFloat(element.getAttribute('data-delay') || '0');
      const stagger = parseFloat(element.getAttribute('data-stagger') || '0.08');
      
      element.innerHTML = words.map((word, index) => {
        const delay = baseDelay + (index * stagger);
        return `<span class="blur-reveal-word" style="animation-delay: ${delay}s">${word}</span>`;
      }).join(' ');
    });
  }
  
  // Ejecutar cuando el DOM esté listo
  document.addEventListener('DOMContentLoaded', initBlurReveal);
  // También ejecutar inmediatamente por si ya está cargado
  if (document.readyState !== 'loading') {
    initBlurReveal();
  }
</script>
