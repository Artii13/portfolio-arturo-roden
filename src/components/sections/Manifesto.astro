---
// Manifesto Section - Simplificado para conversión
const points = [
  {
    icon: "⚡",
    title: "Sistemas 24/7",
    description: "Trabajan mientras duermes"
  },
  {
    icon: "🎯",
    title: "Resultados medibles",
    description: "Leads, ventas, tiempo ahorrado"
  },
  {
    icon: "🔗",
    title: "Todo conectado",
    description: "CRM, web, IA, automatizaciones"
  }
];
---

<section id="manifesto" class="py-16 md:py-24">
  <div class="max-w-4xl mx-auto px-6 text-center">
    
    <!-- Headline principal -->
    <h2 class="text-2xl md:text-3xl lg:text-4xl font-medium text-[#0a0a0a] mb-10 md:mb-14 tracking-tight reveal-up">
      La mejor tecnología es la que <span class="text-indigo-600">se hace invisible</span>.
    </h2>
    
    <!-- 3 puntos clave -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-12 mb-12 md:mb-16">
      {points.map((point, index) => (
        <div class="reveal-up" style={`transition-delay: ${index * 0.1}s`}>
          <div class="text-4xl md:text-5xl mb-4">{point.icon}</div>
          <h3 class="text-lg md:text-xl font-semibold text-[#0a0a0a] mb-2">{point.title}</h3>
          <p class="text-gray-500 text-sm md:text-base">{point.description}</p>
        </div>
      ))}
    </div>
    
    <!-- Firma -->
    <div class="reveal-fade">
      <p class="text-gray-400 text-sm italic">
        — Arturo Róden, Founder @ Link IA Lab
      </p>
    </div>
    
  </div>
</section>
